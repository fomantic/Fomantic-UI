language: node_js

node_js:
  - "node"

# Stop as soon as an error is trigerred
matrix:
  fast_finish: true

# Caching node_modules folder for speed up build
cache:
  directories:
    - node_modules

before_install:
  - npm prune
  - sh preinstall.sh
